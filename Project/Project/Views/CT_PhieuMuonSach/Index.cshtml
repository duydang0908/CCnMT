@model IEnumerable<Project.CT_PhieuMuonSach>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div style="text-align:left">Hello, Administrator</div>
<a href="~/Admin/Logout">Logout</a>
<h2>CHI TIẾT PHIẾU MƯỢN</h2>

<table class="table" border="1">
    <tr>
        <th>
            Mã phiếu mượn
        </th>
        <th>
            Tên sách
        </th>
        <th>
            Số lượng
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PhieuMuonSach.MaPhieuMuon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SACH.Tensach)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoLuong)
            </td>
            <td>
                @Html.ActionLink("Xóa", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
